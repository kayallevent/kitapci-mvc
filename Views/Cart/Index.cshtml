@model List<Basics.Models.Kitap>
    

@{
    ViewBag.Title = "Sepetim";
}

<h2>Sepetinizdeki Kitaplar</h2>

@if (Model.Count == 0)
{
    <p>Sepetiniz boş.</p>
}
else
{
    <ul>
        @foreach (var kitap in Model)
        {
            <li>
                Kitap ID: @kitap.Id 
                <a href="/Cart/AddToCart/@kitap.Id" class="btn-sepet">🛒 Sepete Ekle</a>

            </li>
        }
    </ul>
}
